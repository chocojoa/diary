// 주의: 이 파일은 git에 커밋됩니다 (빈값 상태로).
// API 키는 반드시 빈값으로 유지하고, 절대 하드코딩하지 마세요.
// 앱 실행 후 앱 > 설정 화면에서 API 키를 입력하세요.

class ApiConfig {
  // Google Gemini API 키
  // 기본값: 비워두고 설정 화면에서 입력
  static const String googleApiKey = '';

  // 참고: 실제 환경에서는 다음과 같이 설정할 수 있습니다:
  // static const String googleApiKey = 'YOUR_API_KEY_HERE';
}
